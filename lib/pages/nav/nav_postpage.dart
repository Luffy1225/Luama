import 'package:flutter/material.dart';

class Nav_PostWidget extends StatelessWidget {
  const Nav_PostWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Post");
  }
}

import 'package:flutter/material.dart';

class Nav_ProfileWidget extends StatelessWidget {
  const Nav_ProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("data");
  }
}
